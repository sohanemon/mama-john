import { Outlet, Route, Routes } from "react-router-dom";

const Inventory = () => {
  return (
    <div>
      emon
      <div>
        <Outlet />
      </div>
    </div>
  );
};

export default Inventory;
